export enum TokenType {
	TOKEN = "TOKEN",
	INTEGER = "INTEGER",
	MULTIPLY = "MULTIPLY",
	DIVISION = "DIVISION",
	LPAREN = "LPAREN",
	RPAREN = "RPAREN",
	LBRACE = "LBRACE",
	RBRACE = "RBRACE",
	PLUS = "PLUS",
	MINUS = "MINUS",
	SEMI = "SEMI",
	ID = "ID",
	EOF = "EOF",
	EOL = "EOL",
}

export default TokenType;